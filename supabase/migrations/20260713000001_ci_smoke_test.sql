-- Runway — CI pipeline smoke test. No-op (comment only): confirms the
-- GitHub Actions "Deploy Supabase" workflow applies new migrations
-- automatically on push, after the one-time baseline repair.
select 1;
